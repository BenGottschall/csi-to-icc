# Docker Environment Configuration
# This file contains environment variables for running the application in Docker

# Database Configuration (PostgreSQL in Docker)
DATABASE_URL=postgresql://postgres:postgres@db:5432/csi_icc_db

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# CORS Configuration (Allow frontend to access backend)
CORS_ORIGINS=http://localhost:3000,http://localhost

# Optional: ICC Credentials for scraping (if needed)
# ICC_USERNAME=your_email@example.com
# ICC_PASSWORD=your_password
